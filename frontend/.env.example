# Frontend API base URL
# Leave empty to use same-origin when frontend is served by FastAPI static files.
# Deployed example (Vercel frontend -> Render backend):
# VITE_API_BASE_URL=https://your-backend-service.onrender.com
VITE_API_BASE_URL=
